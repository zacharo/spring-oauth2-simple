# Simple Spring oAuth2 server

This project contains minimalistic oAuth2 server using the Spring
framework. The server is configured to be both the Authorization and
Resource provider at the same time.
